# Agent Notes

## Keyboard shortcut changes

Before changing a keyboard shortcut that is already bound, stop and report the
conflict first. State the current binding, the proposed new behavior, what would
be displaced, and a small set of options. Do not silently move or replace an
existing top-level binding.
